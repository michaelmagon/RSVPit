Home


<% if user_signed_in? %>
<div class="flex flex-row gap-2">
  <%= link_to "Manage", rails_admin_path, class: "btn btn-primary" %>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
</div>
<% else %>
  <%= link_to "Sign in", new_user_session_path %>
<% end %>